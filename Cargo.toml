[package]
name = "collatz-weyl"
version = "0.1.0"
edition = "2024"

[features]
rand-core = ["rand_core"]

[dependencies]
rand_core = {version = "0.10.0", optional = true}

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "throughput"
harness = false